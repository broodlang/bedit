;; myedit — a super-minimal GUI text editor, built on the rope-backed buffer
;; framework (editor/buffer) + the ui-run loop (editor/ui). The M2 editor app:
;; a standalone `nest` project that `(:use editor/buffer)`s the bundled editor
;; framework. See src/main.blsp.
;;
;; Run it (needs a GUI-enabled `nest` — `./configure --with-gui && make install`
;; in the brood repo):
;;     nest run                 ; a scratch buffer
;;     nest run -- notes.txt    ; open (Ctrl-S saves) that file
;;
;; net/* and proc/supervisor are internal packages (ADR-085 Move 2): put their
;; src on the load-path directly — no package manager needed in-workspace.
(project :name "myedit"
  :version "0.1.0"
  :source-paths ["src" "../brood-net/src" "../brood-supervisor/src"])
