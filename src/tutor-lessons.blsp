(defmodule tutor-lessons
  "The Brood tutorial's CONTENT, nothing else: `tutor-lessons` is a vector of
lesson maps the machinery in `tutor` renders and checks. Grow the course by
editing this file only.

A lesson: `{:title str :body [line …] :playgrounds [pg …]}` — `:body` lines are
pre-wrapped prose (an empty string is a blank line). A playground:
`{:title str :code str}` plus, for an exercise, `:expect {:value <pr-str>}` (the
LAST form's value, as `pr-str` renders it — a string result is `\"\\\"hi\\\"\"`)
and an optional `:hint` shown on a miss; `:expect nil` means show-the-result-only.
`:trace [\"fn-name\" …]` boundary-traces those names in the sandbox and renders
the call/return cascade in the *Workings* side pane. Definitions persist down the
lesson (playgrounds re-evaluate top-to-bottom in one session), so a later
playground may call what an earlier one defined.")

(def tutor-sections
  ;; The course as SECTIONS, each optionally split into ordered SUBSECTIONS. The user
  ;; walks Navigation first, then chooses from the contents page
  ;; (`tutor/cmd-tutorial-contents`). `:id` matches each lesson's `:section`, and a
  ;; subsection's `:id` matches its `:subsection` — lessons are grouped by both, so a
  ;; long topic (the language) reads as a few short walks instead of one long one.
  [{:id :navigation :title "Navigation"
    :blurb "Move around like you mean it — keys, leaps, and search."}
   {:id :editing :title "Editing"
    :blurb "Change text, undo, and pick the keys you drive (emacs or vim)."}
   {:id :language :title "The Brood language (.blsp)"
    :blurb "The language itself: values, functions, data, and processes."
    :subsections [{:id :basics :title "Values & expressions"
                   :blurb "Numbers, strings, let, if — and lists vs vectors."}
                  {:id :functions :title "Functions & data"
                   :blurb "defn, immutability, map/filter/fold, recursion as the loop."}
                  {:id :matching :title "Pattern matching & errors"
                   :blurb "match on shape, destructuring, and try/catch."}
                  {:id :shapes :title "Records & abilities"
                   :blurb "Your own types, and one name that works across all of them."}
                  {:id :macros :title "Code is data"
                   :blurb "Quoting, eval, and writing a macro that adds syntax."}
                  {:id :names :title "Names & namespaces"
                   :blurb "Modules, privacy, and dynamic variables you can rebind."}
                  {:id :processes :title "Processes"
                   :blurb "spawn, send, receive — state that genuinely changes."}]}
   {:id :editor :title "The editor is Brood"
    :blurb "Evaluate in place, redefine the editor live, and where to go next."}])

(def tutor-lessons
  [;; ---- 1 · the choice that shapes everything after it ----------------------------
   {:title "Choose your keys"
    :section :navigation
    :body ["Welcome! This buffer is alive: everything inside a box is yours to"
           "edit — the prose around it is not. Two kinds of box, and the border"
           "tells you which: ┄ dashed holds plain TEXT, ─ solid holds real Brood"
           "that evaluates as you type. This section is all text; code starts once"
           "you can move around. A ✓ or ✗ in the margin marks an exercise either"
           "way, and the echo line says why."
           ""
           "First, the keys. You are driving the {{profile-name}} profile right now,"
           "and this whole course renders for whichever you pick:"
           ""
           "  · emacs — chords: C-f forward, C-k kill a line, M-x runs a command."
           "  · vim — modal: hjkl to move, i to insert, ESC back, : runs a command."
           ""
           "Switch with {{kcmd}} vim-mode or {{kcmd}} emacs-mode (C-x v v does either"
           "way). Do it now if you like: every lesson, this page included, re-renders"
           "its keys at once and keeps whatever you have typed in the boxes."
           ""
           "The tutorial's OWN keys never change — {{next-box}}/{{prev-box}} move"
           "between boxes, {{next-lesson}}/{{prev-lesson}} between lessons — those"
           "run straight through the whole course, section after section, the way"
           "Emacs Info reads. {{up}} goes UP to the contents (landing on where you"
           "were), {{last-page}} retraces your steps, {{reset-box}} resets a box."]
    :playgrounds [{:kind :text
                   :title "Try it — type in here, it is yours ({{profile-name}} keys)"
                   :code "Type anything. Change this line, add another.\nSwitch profiles while you are in here: what you\ntyped stays exactly where it is."}
                  {:kind :text
                   :title "Exercise — make this line read: my keys are {{profile-name}}"
                   :code "my keys are"
                   :expect {:text "my keys are {{profile-name}}"}
                   :hint "put the cursor at the end of the line and type the name"}]}

   ;; ---- 2 · the editor itself: moving around -----------------------------------
   {:title "Moving around"
    :section :navigation
    :body ["Now move around. The arrows always work; so do the {{profile-name}} keys:"
           "{{move-line}}."
           "M-n hops to the next box, M-p (or S-TAB) hops back — and the"
           "prose can't be edited, only the boxes can."
           "{{insert-hint}}"]
    :playgrounds [{:kind :text
                   :title "Practice — plain text, nothing to evaluate: just wander"
                   :code "Move through these lines with {{motion-keys}}.\nNothing in this box is code, so nothing is judged —\nit only cares that you can get around it.\nEnd of this line, then the start of the next one."}
                  {:kind :text
                   :title "Exercise — fix the typo on the SECOND line: dgo should be dog"
                   :code "the quick brown fox\njumps over the lazy dgo"
                   :expect {:text "the quick brown fox\njumps over the lazy dog"}
                   :hint "{{kn}} down a line, {{keol}} to its end, then fix the two letters"}]}

   ;; ---- 2 · navigation: leaps ----------------------------------------------------
   {:title "Leaps and bounds"
    :section :navigation
    :body ["Bigger jumps: {{kwf}} / {{kwb}} move by WORD, {{kbol}} / {{keol}} to the"
           "line's start and end, {{kbs}} / {{kbe}} to the buffer's very top and bottom,"
           "{{paging-line}}"
           "{{paging-line2}}"]
    :playgrounds [{:kind :text
                   :title "Practice — leap word by word ({{kwf}} / {{kwb}}) along this line"
                   :code "leap over each word in turn then swing all the way back again"}
                  {:kind :text
                   :title "Exercise — delete the word DECOY, leave the rest as it reads"
                   :code "keep this DECOY line intact"
                   :expect {:text "keep this line intact"}
                   :hint "{{kwf}} until you are on it, then delete the word"}]}

   ;; ---- 3 · navigation: search ---------------------------------------------------
   {:title "Search is navigation"
    :section :navigation
    :body ["The fastest way anywhere is to SEARCH for it: {{ksearch}} starts an"
           "incremental search — every keystroke narrows, {{ksearch}} again jumps to"
           "the next match, RET lands the cursor there, C-g cancels back to"
           "where you started. {{krsearch}} searches backward."
           ""
           "Searching beats counting keystrokes: if you can see it, search for it."]
    :playgrounds [{:kind :text
                   :title "Practice — {{ksearch}} for a word you can see, then C-g back"
                   :code "haystack haystack haystack\nhaystack thimble haystack\nhaystack haystack haystack"}
                  {:kind :text
                   :title "Exercise — find the word needle and make it read found"
                   :code "haystack haystack haystack\nhaystack needle haystack\nhaystack haystack haystack"
                   :expect {:text "haystack haystack haystack\nhaystack found haystack\nhaystack haystack haystack"}
                   :hint "{{ksearch}} n e e d l e RET lands the cursor just after it"}]}

   ;; ---- 4 · the editor itself: editing ------------------------------------------
   {:title "Editing"
    :section :editing
    :body ["Everything you'd expect: type to insert, Backspace deletes,"
           "{{kill-line}}"
           "{{kill-line2}}"
           "as many steps as you like. RET auto-indents inside a box."
           "{{insert-hint}}"
           ""
           "If a box gets away from you, C-c C-r resets it to its template."]
    :playgrounds [{:kind :text
                   :title "{{practice-kill}}"
                   :code "kill this line\nbring it back with a yank\nthen undo the whole lot"}
                  {:kind :text
                   :title "Exercise — remove the DELETE ME line, keep the other two"
                   :code "first line stays\nDELETE ME\nlast line stays"
                   :expect {:text "first line stays\nlast line stays"}
                   :hint "{{kill-hint}}"}]}

   {:title "Undo only what you selected"
    :section :editing
    :body ["{{kundo}} undoes the last change. But select some lines first and it"
           "undoes only THOSE lines — the most recent change that touched them —"
           "and leaves every other edit standing. Emacs calls this undo-in-region;"
           "most editors can't do it at all."
           ""
           "Why this editor can: undo here keeps whole previous TEXTS, so the set of"
           "changes is just a line diff away. Undoing a selection means applying"
           "back only the diff hunks inside it — no per-edit bookkeeping needed."
           ""
           "Try it in the box: break line 1 AND line 3 (add junk to each). Then"
           "select just line 1 (C-SPC, then down a line) and press {{kundo}}."
           "Line 1 comes back; your line-3 damage stays. Press {{kundo}} again with"
           "the selection still live and it keeps peeling changes off those lines."]
    :playgrounds [{:kind :text
                   :title "Practice — break line 1 AND line 3, then undo only line 1"
                   :code "first line, damage me\nsecond line, leave me alone\nthird line, damage me too"}]}

   ;; ---- the bridge: the first CODE box, and the reason for it --------------------
   {:title "Moving by structure"
    :section :editing
    :body ["Everything so far was plain text. Here is the first box of real code —"
           "and the reason the editor cares about the difference."
           ""
           "In a Lisp the parentheses ARE the structure, so the cursor can move by"
           "structure instead of by character: C-M-f and C-M-b hop over a whole"
           "( ) form at a time, and C-M-u climbs out of the form you are inside."
           "One key steps over an argument however long it is."
           ""
           "This box is Brood, so it also evaluates as you type — the result"
           "appears at the end of the line. That is the next section's subject."]
    :playgrounds [{:title "Practice — C-M-f from the front, watch it leap whole forms"
                   :code "(list (list 1 2) [3 4] (str \"whole\" \"forms\") :done)"}
                  {:title "Exercise — leap the forms with C-M-f, then make this give 42"
                   ;; no `let` here: this is the EDITING section, and the language
                   ;; section is where bindings are introduced
                   :code "(+ 39 (count [1 2]))"
                   :expect {:value "42"}
                   :hint "C-M-f steps over (count [1 2]) whole — only the number is wrong"}]}

   ;; ---- 5 · the editor itself: your keys -----------------------------------------
   {:title "Your keys, your editor"
    :section :editing
    :body ["You're driving the {{profile-name}} profile — every key this course"
           "teaches renders for it, live. {{kcmd}} opens the command prompt (type"
           "a name, RET): {{kcmd}} vim-mode switches to vim keys, {{kcmd}} emacs-mode"
           "back — and C-x v v toggles either way. Note the prompt key itself"
           "differs: M-x under emacs, : under vim, which is why it is written"
           "as {{kcmd}} here and not spelled out."
           ""
           "The moment you switch, THIS page (and every lesson) re-renders its"
           "keys for the other profile, keeping whatever you typed in the boxes."
           ""
           "The tutorial's own keys — M-n, M-p, TAB, C-c C-n, C-c C-r — live on"
           "this BUFFER's mode, not the profile, so they work under either."
           "Try it now: switch, watch the prose change, and switch back."]
    :playgrounds [{:title "Try it — whichever profile you're in, the box still evaluates"
                   :code "(str \"my keys, \" \"my editor\")"}]}

   ;; ---- 6 · values ----------------------------------------------------------------
   {:title "Values"
    :section :language
    :subsection :basics
    :body ["Now, Brood. Numbers, strings, keywords and booleans evaluate to"
           "themselves; a call is a parenthesised list, operator first: (+ 1 2)."]
    :playgrounds [{:title "Try it — edit the numbers, watch the result follow"
                   :code "(+ 1 2)"}
                  {:title "Exercise — make this evaluate to 42"
                   :code "(* 6 6)"
                   :expect {:value "42"}
                   :hint "six times seven"}]}

   ;; ---- 7 · expressions -----------------------------------------------------------
   {:title "Numbers that stay exact"
    :section :language
    :subsection :basics
    :body ["Divide one by three in most languages and you get 0, or 0.333… — an"
           "answer that is already wrong. Brood gives you the RATIO `1/3`, exactly,"
           "so multiplying it back by three is exactly `1`."
           ""
           "Integers grow as far as they need to; 25 factorial is not a problem and"
           "not an overflow. Floats still exist when you want them — `0.1` is a"
           "float and behaves like one everywhere — but you have to ask."
           ""
           "The rule: exactness is the default, and losing it is a decision you"
           "make rather than one the arithmetic makes for you."]
    :playgrounds [{:title "Try it — exact, then the float you asked for"
                   :code "[(/ 1 3) (* 3 (/ 1 3)) (+ 0.1 0.2)]"}
                  {:title "Try it — an integer that outgrows 64 bits, unremarked"
                   :code "(fold * 1 (range 1 26))"}
                  {:title "Exercise — a third of what is 42?"
                   :code "(* (/ 1 3) 120)"
                   :expect {:value "42"}
                   :hint "the ratio stays exact, so the answer is a whole number"}]}

   {:title "Sets"
    :section :language
    :subsection :basics
    :body ["The third collection. `#{1 2 3}` is a SET: every member appears once,"
           "and membership is the question it answers well."
           ""
           "  (includes? #{:a :b} :a)   ; => true"
           "  (into #{} [1 1 2 2])      ; => #{1 2} — duplicates collapse"
           "  (disj #{1 2 3} 2)         ; => #{1 3}"
           ""
           "A set keeps no order — print one and the members may come back in any"
           "arrangement — so reach for it when you mean \"which things\", and for a"
           "vector when you mean \"these things, in this order\"."]
    :playgrounds [{:title "Try it — uniqueness, membership, removal"
                   :code "[(includes? #{:a :b} :a)\n  (count (into #{} [1 1 2 2 3]))\n  (disj #{1 2 3} 2)]"}
                  {:title "Exercise — the set needs two distinct members"
                   :code "(+ 40 (count (into #{} [1 1 1])))"
                   :expect {:value "42"}
                   :hint "duplicates collapse, so a longer vector may not be a bigger set"}]}

   {:title "Everything is an expression"
    :section :language
    :subsection :basics
    :body ["Three forms arrive at once here, so: what each one IS."
           ""
           "  (def x 41)          name a value for the whole program"
           "  (let (a 1 b 2) …)   name values INSIDE this form only — a flat"
           "                      list of pairs, each seeing the ones before it"
           "  (if test a b)       choose between two values"
           ""
           "`def` and `let` are the same idea at two scopes: `def` puts the name"
           "where everything can see it, `let` puts it where only this form can."
           "Prefer `let` — a name that escapes its use is how programs get hard"
           "to read."
           ""
           "There are no statements. `if`, `let` and `do` are expressions with"
           "values, which is why `let` can be the thing a function returns."
           ""
           "Truthiness: only nil and false are falsy. 0, \"\" and [] are all"
           "true — but the empty LIST () is nil, so it is falsy. Test emptiness"
           "with (empty? x), never with a bare if."]
    :playgrounds [{:title "Try it"
                   :code "(let (x 10\n      y (* x 2))\n  (if (> y 15) :big :small))"}
                  {:title "Exercise — bind `name` so the greeting reads \"hello, brood\""
                   :code "(let (name \"world\")\n  (str \"hello, \" name))"
                   :expect {:value "\"hello, brood\""}
                   :hint "change what `name` is bound to"}]}

   ;; ---- 8 · lists and vectors -----------------------------------------------------
   {:title "Lists and vectors"
    :section :language
    :subsection :basics
    :body ["Lists ( ) are code — calling them is what evaluation is. Vectors"
           "[ ] are data: [1 2 3] evaluates to itself. Both are immutable and"
           "share one sequence API: first, rest, count, map, filter, fold."
           ""
           "conj adds at a collection's natural point: the front of a list,"
           "the end of a vector. It returns a NEW collection — nothing mutates."]
    :playgrounds [{:title "Try it"
                   :code "(let (v [1 2 3])\n  [(first v) (count v) (conj v 4)])"}
                  {:title "Exercise — produce [10 20 30 40] from the vector below"
                   :code "(conj [10 20 30] 4)"
                   :expect {:value "[10 20 30 40]"}
                   :hint "conj appends to a vector — what belongs at the end?"}]}

   ;; ---- 9 · functions (traced: the *Workings* pane debuts) ------------------------
   {:title "Strings and keywords"
    :section :language
    :subsection :basics
    :body ["Two value types you will reach for constantly."
           ""
           "Strings are ordinary values — `upper`, `string-length`, `substring`,"
           "`str` to build one. Keywords are self-naming constants: `:score` means"
           "itself and nothing else, which makes them perfect map keys."
           ""
           "The trick worth knowing: a keyword IS a function of a map. `(:score p)`"
           "looks up `:score` in `p`. That is why so much Brood reads as data being"
           "asked questions rather than objects being told things."]
    :playgrounds [{:title "Try it — strings, and a keyword asking a map"
                   :code "[(upper \"brood\") (string-length \"brood\") (:score {:score 42})]"}
                  {:title "Exercise — the keyword is misspelt; make this give \"brood\""
                   :code "(name :broo)"
                   :expect {:value "\"brood\""}
                   :hint "`name` turns a keyword into its string — fix the keyword itself"}]}

   {:title "Functions"
    :section :language
    :subsection :functions
    :body ["`fn` makes a function; `def` names a value; `defn` is the two in one"
           "breath, and that is the whole difference:"
           ""
           "  (def double (fn (x) (* 2 x)))    the long way"
           "  (defn double (x) (* 2 x))        the same thing, said once"
           ""
           "So a function is just a value with a name — which is why one can be"
           "passed to `map`, returned from another function, or redefined while"
           "the editor runs. The last form's value is what it returns; there is"
           "no `return`."
           ""
           "Parameter lists are ( ), not [ ] — code is lists, data is vectors."
           ""
           "A definition made in one box is callable in the boxes below it"
           "(the whole lesson shares one live session, top to bottom)."
           ""
           "These boxes are TRACED: the *Workings* pane beside shows every call"
           "and return as the sandbox ran it — watch it follow your edits."]
    :playgrounds [{:title "Try it — define…"
                   :code "(defn double (x) (* 2 x))"
                   :trace ["double"]}
                  {:title "…and call (this box sees `double` from the box above)"
                   :code "(double 21)"
                   :trace ["double"]}
                  {:title "Exercise — fix `triple` so the call below returns 42"
                   :code "(defn triple (x) (+ x 3))\n(triple 14)"
                   :expect {:value "42"}
                   :trace ["triple"]
                   :hint "triple means times three, not plus three"}]}

   ;; ---- 10 · immutability ----------------------------------------------------------
   {:title "Immutability"
    :section :language
    :subsection :functions
    :body ["Nothing mutates. assoc, dissoc, update, conj all return a fresh"
           "value; the original is untouched. There is no set! and no loops —"
           "changing state means computing a new value from the old one."
           ""
           "Maps are written {:key value …}, no commas. (:key m) reads a key"
           "— keywords are callable."]
    :playgrounds [{:title "Try it — the original map survives"
                   :code "(let (m {:name \"ada\" :age 36})\n  [(assoc m :age 37) m])"}
                  {:title "Exercise — read the :score of the player AFTER raising it to 100"
                   :code "(let (player {:name \"zed\" :score 40})\n  (:score player))"
                   :expect {:value "100"}
                   :hint "(:score (assoc player :score 100)) — assoc gives you the new map"}]}

   ;; ---- 11 · map, filter, fold -----------------------------------------------------
   {:title "map, filter, fold"
    :section :language
    :subsection :functions
    :body ["Iteration is a function you apply, not a loop you write:"
           "  (map f xs)      — transform every element"
           "  (filter p xs)   — keep the elements p likes"
           "  (fold f acc xs) — thread an accumulator through the sequence"
           ""
           "fold is the general one — map and filter could each be written"
           "with it. (range n) is the integers 0 … n-1."
           ""
           "Combine them by nesting: the innermost call runs first, so you read"
           "these from the INSIDE out, right to left. That is backwards from the"
           "order things happen, and it is exactly what the next lesson fixes."]
    :playgrounds [{:title "Try it — squares of the even numbers, read inside-out"
                   :code "(map (fn (x) (* x x))\n  (filter even? (range 10)))"}
                  {:title "Exercise — now SUM them, so this gives 120"
                   :code "(map (fn (x) (* x x))\n  (filter even? (range 10)))"
                   :expect {:value "120"}
                   :hint "wrap the whole thing: (fold + 0 …) adds a sequence up"}]}

   ;; ---- 12 · recursion (traced) ---------------------------------------------------
   {:title "Threading it together"
    :section :language
    :subsection :functions
    :body ["The last lesson ended with a call inside a call inside a call, read"
           "right to left. That is the problem this lesson solves: two macros put"
           "the steps back into the order they actually happen:"
           ""
           "  (-> 5 (+ 1) (* 7))         ; 5, then +1, then ×7 — value goes FIRST"
           "  (->> xs (filter odd?) …)   ; …the collection goes LAST"
           ""
           "`->` threads into the first argument, `->>` into the last. That split"
           "is not arbitrary: Brood's value-shaped functions take the value first"
           "(`assoc`, `str`), and its sequence functions take the sequence last"
           "(`map`, `filter`, `fold`). Pick the arrow that matches."]
    :playgrounds [{:title "Try it — the same pipeline, written the way it runs"
                   :code "(-> 5 (+ 1) (* 7))"}
                  {:title "Exercise — make the doubled numbers sum to 42"
                   :code "(->> [1 2 3]\n  (map (fn (x) (* x 2)))\n  (fold + 0))"
                   :expect {:value "42"}
                   :hint "every number doubles, then they add up — the parts must total 21"}]}

   {:title "Functions that make functions"
    :section :language
    :subsection :functions
    :body ["A function can return a function, and the returned one remembers the"
           "values it was built with. That memory is a CLOSURE."
           ""
           "  (defn adder (n) (fn (x) (+ x n)))"
           "  ((adder 40) 2)   ; => 42"
           ""
           "Nothing was stored and nothing mutated: `n` is simply still in scope"
           "for the inner function. This is how you configure behaviour in a"
           "language without objects — and `map`/`filter`/`fold` are all just"
           "functions taking functions."]
    :playgrounds [{:title "Try it — one maker, two different functions"
                   :code "(defn adder (n)\n  (fn (x) (+ x n)))\n[((adder 40) 2) ((adder 1) 1)]"}
                  {:title "Exercise — make the inner function actually use `n`"
                   :code "(defn scaler (n)\n  (fn (x) x))\n((scaler 6) 7)"
                   :expect {:value "42"}
                   :hint "the inner fn can see n — multiply by it"}]}

   {:title "Recursion is the loop"
    :section :language
    :subsection :functions
    :body ["Brood guarantees proper tail calls: a function calling itself in"
           "tail position runs in constant stack, forever if it likes. That is"
           "the loop. Multi-clause defn dispatches on the argument's shape —"
           "the base case is just another clause."
           ""
           "The exercise is traced: *Workings* shows the whole cascade of"
           "recursive calls unwinding back up — the shape of recursion itself."]
    :playgrounds [{:title "Try it — count down by matching on 0 (250,000 tail calls, O(1) stack)"
                   ;; 250k, not the million this used to promise. Measured: the same
                   ;; loop is 205ms compiled and 2962ms through `eval` of a read form,
                   ;; which is the sandbox's path — so a million calls ran past the 2s
                   ;; box budget and the lesson greeted the reader with "timed out".
                   ;; (Output capture is NOT the cost: 189ms with it, 205ms without.
                   ;; `eval` runs interpreted; making it compile is a runtime change.)
                   ;; The point — constant stack, no overflow — needs a big number,
                   ;; not a specific one.
                   :code "(defn countdown\n  ((0) :liftoff)\n  ((n) (countdown (- n 1))))\n(countdown 250000)"}
                  {:title "Exercise — finish factorial (5! is 120)"
                   :code "(defn fac\n  ((0) 1)\n  ((n) n))\n(fac 5)"
                   :expect {:value "120"}
                   :trace ["fac"]
                   :hint "the (n) clause should multiply n by the factorial of (n - 1)"}]}

   ;; ---- 13 · processes ------------------------------------------------------------
   ;; ---- pattern matching & errors (:language / :matching) --------------------------
   {:title "Watching it run"
    :section :language
    :subsection :functions
    :body ["When a result surprises you, look inside instead of guessing."
           ""
           "`spy` prints a value and returns it UNCHANGED, so you can wrap any"
           "expression mid-pipeline without disturbing what it computes — the"
           "printed text shows up beside the result here."
           ""
           "The box below goes further: this lesson asks the tutorial to TRACE two"
           "functions, so every call and return opens in a *Workings* pane beside"
           "the page. Seeing `sum-doubles` call `double-` once per element makes"
           "`fold` concrete in a way that reading it does not."]
    :playgrounds [{:title "Try it — watch one function call another (see *Workings*)"
                   :code "(defn double- (x) (* x 2))\n(defn sum-doubles (xs)\n  (fold (fn (acc x) (+ acc (double- x))) 0 xs))\n(sum-doubles [1 2 3])"
                   :trace ["double-" "sum-doubles"]}
                  {:title "Exercise — spy reports the halfway value; make the answer 42"
                   :code "(defn twice-it (x)\n  (spy (* x 2)))\n(twice-it 20)"
                   :expect {:value "42"}
                   :hint "spy changes nothing — the input is one short"}]}

   {:title "Matching on shape"
    :section :language
    :subsection :matching
    :body ["`match` picks a branch by the SHAPE of a value, binding as it goes."
           "A bare symbol BINDS (it doesn't compare) — to match a value you"
           "already have, pin it with ^. Vectors are the tagged-data idiom:"
           "[:circle 2] and [:rect 3 4] tell themselves apart."
           ""
           "`_` matches anything. Clauses are tried top to bottom."]
    :playgrounds [{:title "Try it — edit the shape and watch the branch change"
                   :code "(let (shape [:rect 3 4])\n  (match shape\n    ([:circle r] (* 3 r r))\n    ([:rect w h] (* w h))\n    (_ :unknown)))"}
                  {:title "Exercise — add a [:square s] clause so this gives 42… well, 49"
                   :code "(let (shape [:square 7])\n  (match shape\n    ([:rect w h] (* w h))\n    (_ :unknown)))"
                   :expect {:value "49"}
                   :hint "add ([:square s] (* s s)) above the _ clause"}]}

   {:title "Destructuring"
    :section :language
    :subsection :matching
    :body ["The same patterns work in `let` and in function parameters:"
           "(let ([x y] point) …) unpacks a 2-vector, {:keys [a b]} pulls"
           "keys out of a map, and (first & rest) splits a list."
           ""
           "Reach for it instead of a pile of `get` calls — the shape of the"
           "data shows up in the code that reads it."]
    :playgrounds [{:title "Try it"
                   :code "(let (point [3 4]\n      {:keys [name age]} {:name \"ada\" :age 36}\n      [x y] point)\n  [name age (+ x y)])"}
                  {:title "Exercise — destructure the map so this returns the score, 42"
                   :code "(let (player {:name \"zed\" :score 42})\n  0)"
                   :expect {:value "42"}
                   :hint "(let ({:keys [score]} player) score) — or just (:score player)"}]}

   {:title "When things go wrong"
    :section :language
    :subsection :matching
    :body ["`try` runs a body and `catch` takes ONE binder for whatever was"
           "thrown — never a type: (try (risky) (catch e …)). Use"
           "(error-message e) to read it, whatever shape it came in."
           ""
           "`throw` sends any value; `error` builds a message and throws it."
           "Brood's rule of thumb: throw for bugs, return a tagged vector"
           "([:ok v] / [:error e]) for outcomes a caller should branch on."]
    :playgrounds [{:title "Try it — a caught error is just a value again"
                   :code "(try\n  (error \"boom: \" 7)\n  (catch e (str \"caught -> \" (error-message e))))"}
                  {:title "Exercise — make the division safe, returning :nope instead of throwing"
                   :code "(defn safe-div (a b)\n  (/ a b))\n(safe-div 1 0)"
                   :expect {:value ":nope"}
                   :starts-with-error? true   ; the crash IS the lesson here
                   :trace ["safe-div"]
                   :hint "wrap the body: (try (/ a b) (catch e :nope))"}]}

   ;; ---- records & abilities ------------------------------------------------------
   {:title "Your own shapes"
    :section :language
    :subsection :shapes
    :body ["A map takes whatever keys you put in it, and nothing ever tells you"
           "when you spell one wrong — `(:scoer p)` is just nil. `defrecord` fixes"
           "the fields up front:"
           ""
           "  (defrecord Point (x y))"
           ""
           "You get a constructor `(Point 3 4)` that insists on both fields, so a"
           "missing one is an error where you BUILD the value rather than a nil"
           "somewhere far away. You get `(Point-x p)` beside the plain `(:x p)`,"
           "and that named accessor is the one that catches typos: `(Point-z p)`"
           "is an unbound symbol, where `(:z p)` still just answers nil."
           ""
           "It is an ordinary immutable value otherwise — `assoc` returns a new one,"
           "and it can hold keys you never declared. A record is a shape you have"
           "NAMED, not a wall. The name is the part that matters: it travels with"
           "the value, which is how the next lesson dispatches on it."]
    :playgrounds [{:title "Try it — a record is a value like any other"
                   :code "(defrecord Point (x y))\n(let (p (Point 3 4))\n  [(:x p) (Point-y p) (:y (assoc p :y 99))])"}
                  {:title "Exercise — give Point a `moved` that shifts x by 2, so this is 42"
                   :code "(defn moved (p)\n  p)\n(:x (moved (Point 40 0)))"
                   :expect {:value "42"}
                   :hint "assoc returns a NEW point: (assoc p :x (+ 2 (:x p)))"}]}

   {:title "One name, many shapes"
    :section :language
    :subsection :shapes
    :body ["An ABILITY is a name that different shapes answer differently — Brood's"
           "polymorphism, and how the editor dispatches its own events."
           ""
           "  (defability Shape (area [s]))         ; the name and its signature"
           "  (impl Shape Circle (area [s] …))      ; how a Circle answers it"
           ""
           "Then `(area x)` works for anything that implements it. Adding a new"
           "shape later never touches the code that calls `area` — that is the"
           "whole point: the caller names WHAT it wants, not who can do it."]
    :playgrounds [{:title "Try it — two shapes, one call site"
                   :code "(defability Shape (area [s]))\n(defrecord Sq (side))\n(impl Shape Sq (area [s] (* (Sq-side s) (Sq-side s))))\n(area (Sq 5))"}
                  {:title "Exercise — teach Rect the same ability, so this gives 42"
                   :code "(defrecord Rect (w h))\n\n(area (Rect 6 7))"
                   :expect {:value "42"}
                   :starts-with-error? true   ; "no impl for :Rect" is the instruction
                   :hint "(impl Shape Rect (area [s] (* (Rect-w s) (Rect-h s))))"}]}

   ;; ---- code is data --------------------------------------------------------------
   {:title "Code is data"
    :section :language
    :subsection :macros
    :body ["A Brood program is made of the same lists and symbols you have been"
           "building all along. Quote a form with `'` and it stays data instead of"
           "running; hand that data to `eval` and it runs after all."
           ""
           "  '(+ 1 2)        ; a three-element list: the symbol + and two numbers"
           "  (eval '(+ 1 2)) ; => 3"
           ""
           "This is not a party trick. It is why the editor can evaluate the buffer"
           "you are reading, and why the next lesson can add syntax to the language."]
    :playgrounds [{:title "Try it — the same form, as data and as code"
                   :code "(let (form '(+ 40 2))\n  [(first form) (count form) (eval form)])"}
                  {:title "Exercise — build the form (* 6 7) with `list`, then run it"
                   :code "(eval (list))"
                   :expect {:value "42"}
                   :hint "(list '* 6 7) is the data; eval turns it into the answer"}]}

   {:title "Writing a macro"
    :section :language
    :subsection :macros
    :body ["A macro is a function that runs BEFORE your program does, and whose"
           "answer is code. Where a function receives values, a macro receives the"
           "unevaluated forms themselves — so it can decide whether its argument"
           "runs at all, or runs twice."
           ""
           "  (defmacro skip-if (test body)"
           "    (list 'if test nil body))     ; returns a form; that form then runs"
           ""
           "`skip-if` could not be a function: a function's arguments are all"
           "evaluated before the call, so the body would run either way."
           ""
           "Three boxes. The first uses a macro; the second SHOWS you what a macro"
           "is handed, which is the part that makes the rest obvious; the third"
           "asks you to build a form yourself."]
    :playgrounds [{:title "Try it (1/3) — new syntax, defined in three lines"
                   :code "(defmacro skip-if (test body)\n  (list 'if test nil body))\n(skip-if false :it-ran)"}
                  {:title "Try it (2/3) — what does a macro actually receive?"
                   :code "(defmacro show-me (body)\n  (list 'quote body))\n(show-me (+ 1 2))"}
                  {:title "Exercise (3/3) — make `twice` add its body's value to itself"
                   :code "(defmacro twice (body)\n  body)\n(let (n 21)\n  (twice (+ n 0)))"
                   :expect {:value "42"}
                   :hint "box 2 showed `body` is a form — return a bigger form built with `list`"}]}

   ;; ---- names ---------------------------------------------------------------------
   {:title "Modules and privacy"
    :section :language
    :subsection :names
    :body ["Every file is a MODULE. It declares its name and what it borrows:"
           ""
           "  (defmodule my/thing"
           "    (:use editor/buffer))    ; use its names unqualified"
           ""
           "Anything else you reach by qualifying it — `theme/*green*`. A name with"
           "a `--` in it is PRIVATE to its module (`tutor--set-note`), and calling"
           "one from outside is an error, not a convention. That is why this editor"
           "can be read one file at a time: each says what it depends on."]
    :playgrounds [{:title "Try it — require a module, then call it by its full name"
                   :code "(require 'sexp)\n(count (sexp/top-level-forms \"(+ 1 2) (* 3 4)\"))"}
                  {:title "Exercise — count the forms in the string below, so this gives 42"
                   :code "(require 'sexp)\n(+ 40 (count (sexp/top-level-forms \"(+ 1 2)\")))"
                   :expect {:value "42"}
                   :hint "the qualified call counts top-level forms — give it one more"}]}

   {:title "Variables that follow the call"
    :section :language
    :subsection :names
    :body ["Sometimes a value should be ambient: the output stream, the theme, the"
           "timeout — something deep in a call chain needs it, and threading it"
           "through every function between would be noise."
           ""
           "  (defdyn *volume* 3)              ; a dynamic variable, with a default"
           "  (binding (*volume* 11) (loud))   ; rebound for this call only"
           ""
           "`binding` is not mutation: the old value is back the moment the form"
           "ends, and only this call chain ever saw the new one. The stars in the"
           "name are a convention that says \"expect this to be rebound\"."]
    :playgrounds [{:title "Try it — inside and outside the binding"
                   :code "(defdyn *volume* 3)\n(defn hear () *volume*)\n[(hear) (binding (*volume* 11) (hear)) (hear)]"}
                  {:title "Exercise — rebind *volume* so this reads 42"
                   :code "(defn hear () *volume*)\n(binding (*volume* 2)\n  (* (hear) 14))"
                   :expect {:value "42"}
                   :hint "the binding decides what `hear` sees — and 3 × 14 is 42"}]}

   {:title "Processes"
    :section :language
    :subsection :processes
    :body ["Everything so far has been values that never change. State that"
           "genuinely changes lives in a PROCESS — Erlang's model, Brood's"
           "syntax. Four words do all of it:"
           ""
           "  (self)          who am I — a process id you can be sent things at"
           "  (spawn expr)    run expr in a NEW process, with its own heap"
           "  (send pid msg)  post a message; it never blocks"
           "  (receive …)     wait for one, matching on its shape"
           ""
           "Nothing is shared between processes, so there is nothing to lock and"
           "nothing to race. Three boxes below, each adding one idea: hear from a"
           "child, then match what it says, then make it compute."
           ""
           "Every receive here ends with (after ms …) — the escape hatch, so a"
           "message that never arrives times out instead of hanging the box."]
    :playgrounds [{:title "Try it (1/3) — a child sends, you hear it"
                   :code "(let (me (self))\n  (spawn (send me :hello))\n  (receive\n    (msg msg)\n    (after 500 :nothing-arrived)))"}
                  {:title "Try it (2/3) — the same, but matching a TAGGED message"
                   :code "(let (me (self))\n  (spawn (send me [:pong 42]))\n  (receive\n    ([:pong x] x)\n    (after 500 :lost)))"}
                  {:title "Exercise (3/3) — the child does the work: reply with double 21"
                   :code "(let (me (self))\n  (spawn (send me [:answer 21]))\n  (receive\n    ([:answer x] x)\n    (after 500 :lost)))"
                   :expect {:value "42"}
                   :hint "the child decides what it sends — it can compute before sending"}]}

   {:title "A process that remembers"
    :section :language
    :subsection :processes
    :body ["A process is how you get mutable state without mutation. It LOOPS,"
           "holding its state as an argument; each message produces the next"
           "state by calling itself again. Nothing is assigned; the state simply"
           "becomes the argument of the next turn."
           ""
           "  (defn counter (n)"
           "    (receive"
           "      ([:add k from] (do (send from (+ n k))     ; answer"
           "                       (counter (+ n k))))       ; …and carry on"
           "      (after 1000 n)))"
           ""
           "The recursive call is in tail position, so the loop runs forever in"
           "constant stack — a process can wait for years without growing."
           ""
           "First box defines it and asks once; second asks TWICE, which is how"
           "you see that it remembered."]
    :playgrounds [{:title "Try it (1/3) — define the loop, ask it once"
                   :code "(defn counter (n)\n  (receive\n    ([:add k from] (do (send from (+ n k))\n                     (counter (+ n k))))\n    (after 1000 n)))\n(let (me (self)\n      c (spawn (counter 0)))\n  (send c [:add 40 me])\n  (receive (total total) (after 1000 :lost)))"}
                  {:title "Try it (2/3) — ask twice: the second answer proves it remembered"
                   :code "(let (me (self)\n      c (spawn (counter 0)))\n  (send c [:add 40 me])\n  (let (first-answer (receive (x x) (after 1000 :lost)))\n    (send c [:add 2 me])\n    (let (second-answer (receive (x x) (after 1000 :lost)))\n      [first-answer second-answer])))"}
                  {:title "Exercise (3/3) — one message, answer 42: start the counter higher"
                   :code "(let (me (self)\n      c (spawn (counter 0)))\n  (send c [:add 2 me])\n  (receive (x x) (after 1000 :lost)))"
                   :expect {:value "42"}
                   :hint "the starting state is just the argument you spawn it with"}]}

   {:title "When a process dies"
    :section :language
    :subsection :processes
    :body ["Processes are allowed to crash. That is the design: a failure stays"
           "inside the process it happened in, and somebody else decides what to"
           "do about it. You find out by MONITORING."
           ""
           "  (monitor pid)   => a ref, and a promise: when that process ends,"
           "                     [:down ref pid reason] arrives in your mailbox"
           ""
           "The reason tells you which kind of ending it was: :normal for a"
           "process that simply finished, or [:error {…}] for one that blew up."
           ""
           "One piece of syntax: in the pattern ^r means \"the ref already bound"
           "to r\", not a new binding. Without the ^, r would match ANY ref and"
           "you would catch somebody else's monitor. Supervisors are built out"
           "of exactly this, in about ten lines."
           ""
           "The crash box also prints a line of its own — a dying process says"
           "so. That is the runtime talking, not your code."]
    :playgrounds [{:title "Try it (1/3) — a process that finishes cleanly"
                   :code "(let (p (spawn :ok)\n      r (monitor p))\n  (receive\n    ([:down ^r _ reason] reason)\n    (after 500 :lost)))"}
                  {:title "Try it (2/3) — one that crashes, and the reason it gives"
                   :code "(let (p (spawn (error \"i fell over\"))\n      r (monitor p))\n  (receive\n    ([:down ^r _ reason] reason)\n    (after 500 :lost)))"}
                  {:title "Exercise (3/3) — report just the KIND of ending, not the whole reason"
                   :code "(let (p (spawn (error \"i fell over\"))\n      r (monitor p))\n  (receive\n    ([:down ^r _ reason] reason)\n    (after 500 :lost)))"
                   :expect {:value ":error"}
                   :hint "a crash reason is a two-element [:error {…}] — you want its first"}]}

   ;; ---- the editor is Brood (:editor) ----------------------------------------------
   {:title "The editor is a Brood program"
    :section :editor
    :body ["You have reached the point of all this. This editor is about fifty"
           "Brood files — the ones listed in its own README — sitting on top of"
           "the editor toolkit that ships in Brood's standard library:"
           ""
           "  editor/buffer   an immutable buffer: point, movement, editing"
           "  editor/keymap   key → command dispatch"
           "  editor/display  render ops as plain data"
           "  editor/layers   modes as data: keymaps + services"
           ""
           "Nothing is hidden in a binary. The box below loads the SAME buffer"
           "module the editor you are typing into uses, and edits a buffer with"
           "it. Everything the editor does to your text, you can do here."]
    :playgrounds [{:title "Try it — make a buffer and edit it, exactly as the editor does"
                   :code "(require 'editor/buffer)\n(editor/buffer/buffer-text\n  (editor/buffer/insert (editor/buffer/make-buffer \"\") \"hello\"))"}
                  {:title "Exercise — insert twice so the buffer reads \"hello, brood\""
                   :code "(require 'editor/buffer)\n(let (b (editor/buffer/insert (editor/buffer/make-buffer \"\") \"hello\"))\n  (editor/buffer/buffer-text b))"
                   :expect {:value "\"hello, brood\""}
                   :hint "insert returns a NEW buffer — wrap it: (insert b \", brood\")"}]}

   {:title "Keys are data"
    :section :editor
    :body ["A keymap is a map. Nothing more: a key on the left, the NAME of a"
           "command on the right."
           ""
           "  {\"x\" 'delete-char  :ctrl-f 'forward-char}"
           ""
           "Dispatch looks the key up and calls the function that name refers to."
           "Because the right-hand side is a SYMBOL rather than a function value,"
           "the lookup happens at the moment you press the key — which is the"
           "whole trick behind the next lesson."
           ""
           "Your own keys live in ~/.config/brood-edit/init.blsp, and they are"
           "this same shape: data, not API calls."]
    :playgrounds [{:title "Try it — a keymap, a key, a command"
                   :code "(require 'editor/keymap)\n(defn shout (state key) (str state \"!\"))\n(editor/keymap/keymap-dispatch {\"x\" 'shout} \"hi\" \"x\" (fn (s _) s))"}
                  {:title "Exercise — bind \"y\" as well, so pressing it doubles the number"
                   :code "(require 'editor/keymap)\n(defn twice- (state key) (* state 2))\n(editor/keymap/keymap-dispatch {\"x\" 'twice-} 21 \"y\" (fn (s _) s))"
                   :expect {:value "42"}
                   :hint "the keymap is just a map — give it a \"y\" entry too"}]}

   {:title "Redefining a command, live"
    :section :editor
    :body ["Here is why the symbol matters. Dispatch resolves the name on every"
           "keystroke, so redefining the function changes what the key does —"
           "immediately, with nothing restarted and nothing rebound."
           ""
           "That is not a trick for this box. It is how you change this editor:"
           "open src/commands.blsp, edit a `defn`, press C-x C-e on it, and the"
           "very next keypress runs your version. An editor you can repair while"
           "you are sitting in it."
           ""
           "Watch the same key give two different answers below."]
    :playgrounds [{:title "Try it — one key, redefined between two presses"
                   :code "(require 'editor/keymap)\n(def km {\"g\" 'greet})\n(defn greet (state key) \"first version\")\n(let (before (editor/keymap/keymap-dispatch km nil \"g\" (fn (s _) s)))\n  (defn greet (state key) \"second version\")\n  [before (editor/keymap/keymap-dispatch km nil \"g\" (fn (s _) s))])"}
                  {:title "Exercise — redefine `answer` so the SECOND dispatch gives 42"
                   :code "(require 'editor/keymap)\n(def km2 {\"a\" 'answer})\n(defn answer (state key) 0)\n(defn answer (state key) 0)\n(editor/keymap/keymap-dispatch km2 nil \"a\" (fn (s _) s))"
                   :expect {:value "42"}
                   :hint "the LAST definition wins — change the second one"}]}

   {:title "Where next"
    :section :editor
    :body ["You know the language and you know the editor is made of it. Four"
           "things to do next, in the order they pay off:"
           ""
           "1. Ask the editor about itself. C-h k then any key says what that"
           "   key runs; C-h f names a command and shows its documentation."
           "   Every answer is read out of the live editor, so it is never stale."
           ""
           "2. Read one file. C-x p f opens any file in the project by name —"
           "   try src/commands.blsp and search for a command you have pressed."
           "   It will be shorter than you expect."
           ""
           "3. Change something. Put the cursor after a `defn` in that file,"
           "   press C-x C-e, and it is live. Keep what you like by moving it"
           "   into ~/.config/brood-edit/init.blsp, your own config in Brood."
           ""
           "4. Use the rest. C-c t runs the test suite, C-c r runs the project,"
           "   C-x g opens git, C-x w mirrors a buffer to a browser — all of"
           "   them Brood in this same repo, all of them readable."
           ""
           "The tutorial itself is data: src/tutor-lessons.blsp is a vector of"
           "lessons exactly like the ones you have been reading. Add one."]
    :playgrounds [{:title "Try it — ask a keymap what key runs a command (what C-h w does)"
                   :code "(require 'editor/keymap)\n(editor/keymap/keymap-where-is\n  {:ctrl-f 'forward-char :ctrl-x {:ctrl-e 'eval-last-sexp}}\n  'eval-last-sexp)"}]}
])
