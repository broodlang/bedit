* Note from using it

** Projects/Files
- I want to have better file navigation jumping ( layers )
- I want to create a folder  or file
- I want dired thing to manage files
- auto complete must be consistent and a bit nicer. the random hovering does not work for mw
  
** Eval
- I want a shell to run commands in
- A type of compile-mode would be great

** UI
- Mouse click should select text
- Unicode support
- moving the scrollbar is jank or does not work
- scroll accelaration

** Navigation
- negative forward-sexp does not work

** Serve
- it seem like the http socket is not closing properly, just the serving. perhaps a brood issue
