
# runtime crash artifact
.brood_crash_dump

# per-project startup image cache (ADR-218) — rebuilt on demand
.brood/

# local `make build` binary
/bedit

# stale committed release binary — now built on demand via `make install`
/myedit

# editor droppings
\#*\#
.#*

# scratch homes the live pty drivers write (tools/drive_progress.py points
# XDG_CONFIG_HOME here, so a run can never touch your real ~/.config)
/target/
